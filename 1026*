#include <stdio.h>

int main(int argc, char *argv[]){
  long c1, c2;
  scanf("%ld %ld", &c1, &c2);
  int real=(int)((float)(c2-c1)/100+0.5);
  int h = real/3600;
  int m = (real%3600)/60;
  int s = real%60;
  printf("%02d:%02d:%02d", h, m, s);
  return 0;
}
